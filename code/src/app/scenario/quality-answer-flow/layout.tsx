import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "质量回答流程调试",
};

export default function QualityAnswerFlowLayout({ children }: { children: React.ReactNode }) {
  return children;
}
